<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:MermaYT.WinUi.Controls"
    xmlns:converters="using:MermaYT.WinUi.Converters">

    <converters:BytesToFileSizeConverter x:Key="BytesToFileSizeConverter" />
    <converters:DownloadStateToStringConverter x:Key="DownloadStateToStringConverter" />
    <converters:OutputFormatToGlyphConverter x:Key="OutputFormatToGlyphConverter" />

    <Style TargetType="controls:PageHeader">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:PageHeader">
                    <Grid
                        x:Name="PageHeader"
                        Grid.Row="0"
                        Padding="24,12,24,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Spacing="8">
                            <TextBlock
                                x:Name="HeaderTitle"
                                Style="{StaticResource TitleTextBlockStyle}"
                                Text="{TemplateBinding Title}" />

                            <TextBlock
                                x:Name="HeaderSubtitle"
                                FontSize="14"
                                FontWeight="Normal"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Style="{StaticResource SubtitleTextBlockStyle}"
                                Text="{TemplateBinding Subtitle}"
                                Visibility="{TemplateBinding SubtitleVisibility}" />
                        </StackPanel>

                        <Image
                            Grid.Column="1"
                            Width="64"
                            Height="64"
                            VerticalAlignment="Center"
                            Source="ms-appx:///Assets/Square150x150Logo.scale-200.png" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="controls:DownloadListItem">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:DownloadListItem">
                    <Border
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="12">

                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="DownloadStates">
                                <VisualState x:Name="Queued" />
                                <VisualState x:Name="Processing">
                                    <VisualState.Setters>
                                        <Setter Target="DownloadProgressBar.IsIndeterminate" Value="True" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Downloading" />
                                <VisualState x:Name="Converting">
                                    <VisualState.Setters>
                                        <Setter Target="DownloadProgressBar.IsIndeterminate" Value="True" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Paused" />
                                <VisualState x:Name="Completed">
                                    <VisualState.Setters>
                                        <Setter Target="DownloadProgressBar.Visibility" Value="Collapsed" />
                                        <Setter Target="BytesRow.Visibility" Value="Collapsed" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Failed">
                                    <VisualState.Setters>
                                        <Setter Target="DownloadProgressBar.Visibility" Value="Collapsed" />
                                        <Setter Target="BytesRow.Visibility" Value="Collapsed" />
                                    </VisualState.Setters>
                                </VisualState>
                                <VisualState x:Name="Cancelled">
                                    <VisualState.Setters>
                                        <Setter Target="DownloadProgressBar.Visibility" Value="Collapsed" />
                                        <Setter Target="BytesRow.Visibility" Value="Collapsed" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>

                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Border
                                Grid.Column="0"
                                Width="80"
                                Height="80"
                                CornerRadius="4"
                                Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                                <Border.Clip>
                                    <RectangleGeometry Rect="0,0,80,80" RadiusX="4" RadiusY="4" />
                                </Border.Clip>
                                <Image
                                    Source="{Binding Item.ImageUrl, RelativeSource={RelativeSource TemplatedParent}}"
                                    Stretch="UniformToFill" />
                            </Border>

                            <StackPanel
                                Grid.Column="1"
                                Spacing="4"
                                VerticalAlignment="Center">

                                <StackPanel
                                    Orientation="Horizontal"
                                    Spacing="8"
                                    VerticalAlignment="Center">
                                    <TextBlock
                                        Text="{Binding Item.Title, RelativeSource={RelativeSource TemplatedParent}}"
                                        Style="{StaticResource BodyStrongTextBlockStyle}"
                                        TextTrimming="CharacterEllipsis"
                                        VerticalAlignment="Center" />
                                    <Border
                                        Background="{ThemeResource AccentFillColorDefaultBrush}"
                                        CornerRadius="4"
                                        Padding="6,2"
                                        VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal" Spacing="4">
                                            <FontIcon
                                                Glyph="{Binding Item.OutputFormat, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource OutputFormatToGlyphConverter}}"
                                                FontSize="12"
                                                Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}" />
                                            <TextBlock
                                                Text="{Binding Item.OutputFormat, RelativeSource={RelativeSource TemplatedParent}}"
                                                FontSize="12"
                                                Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}" />
                                        </StackPanel>
                                    </Border>
                                </StackPanel>

                                <ProgressBar
                                    x:Name="DownloadProgressBar"
                                    Value="{Binding Item.Progress, RelativeSource={RelativeSource TemplatedParent}}"
                                    Maximum="1" />

                                <StackPanel
                                    x:Name="BytesRow"
                                    Orientation="Horizontal"
                                    Spacing="6">
                                    <TextBlock
                                        Text="{Binding Item.DownloadState, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource DownloadStateToStringConverter}}"
                                        Style="{StaticResource CaptionTextBlockStyle}"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                    <TextBlock
                                        Text="·"
                                        Style="{StaticResource CaptionTextBlockStyle}"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                    <TextBlock
                                        Style="{StaticResource CaptionTextBlockStyle}"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                                        <Run Text="{Binding Item.DownloadedBytes, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BytesToFileSizeConverter}}" />
                                        <Run Text=" of " />
                                        <Run Text="{Binding Item.TotalBytes, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource BytesToFileSizeConverter}}" />
                                    </TextBlock>
                                </StackPanel>

                            </StackPanel>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
