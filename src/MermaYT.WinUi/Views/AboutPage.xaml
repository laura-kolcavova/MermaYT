<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="MermaYT.WinUi.Views.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:MermaYT.WinUi.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel
                Grid.Row="0"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Spacing="8">

                <TextBlock
                    HorizontalAlignment="Center"
                    FontSize="24"
                    FontWeight="SemiBold"
                    Text="MermaYT" />

                <TextBlock
                    Margin="0,0,0,16"
                    HorizontalAlignment="Center"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                    <Run x:Uid="AboutPage_GuiWrapperRun" />
                    <Hyperlink NavigateUri="https://github.com/yt-dlp/yt-dlp ">yt-dlp</Hyperlink>
                </TextBlock>

                <TextBlock
                    x:Uid="AboutPage_DescriptionText"
                    Margin="0,0,0,16"
                    HorizontalAlignment="Center" />

                <TextBlock
                    Margin="0,0,0,8"
                    HorizontalAlignment="Center"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                    <Run x:Uid="AboutPage_IconsAndIllustrationsRun" />
                    <Hyperlink NavigateUri="https://janna.belohradsky.cz/en/index">Janna Bělohradská</Hyperlink>
                </TextBlock>

                <TextBlock HorizontalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                    <Run Text="&#169; 2026" />
                    <Hyperlink NavigateUri="https://github.com/laura-kolcavova">Laura Kolčavová</Hyperlink>
                </TextBlock>
            </StackPanel>

            <Viewbox
                Grid.Row="1"
                Width="300"
                Margin="0,16"
                HorizontalAlignment="Left"
                Visibility="{x:Bind MermaidIconDarkVisibility, Mode=OneWay}">
                <controls:MermaidIconDark BaseColorBrush="{ThemeResource TextFillColorPrimaryBrush}" FillColorBrush="{ThemeResource AccentFillColorDefaultBrush}" />
            </Viewbox>

            <Viewbox
                Grid.Row="1"
                Width="200"
                Margin="0,16"
                HorizontalAlignment="Left"
                Visibility="{x:Bind MermaidIconLightVisibility, Mode=OneWay}">
                <controls:MermaidIconLight BaseColorBrush="{ThemeResource TextFillColorPrimaryBrush}" FillColorBrush="{ThemeResource AccentFillColorDefaultBrush}" />
            </Viewbox>
        </Grid>
    </ScrollViewer>
</Page>
