<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="MermaYT.WinUi.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <Grid Padding="24,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border
                Grid.Row="0"
                Padding="16"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="4">

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <FontIcon
                        Grid.Column="0"
                        Margin="0,0,16,0"
                        VerticalAlignment="Center"
                        FontSize="16"
                        Glyph="{StaticResource GlyphColor}" />

                    <TextBlock
                        x:Uid="SettingsPage_ThemeLabel"
                        Grid.Column="1"
                        VerticalAlignment="Center" />

                    <ComboBox
                        x:Name="ThemeComboBox"
                        Grid.Column="2"
                        MinWidth="160"
                        SelectionChanged="ThemeComboBox_SelectionChanged">
                        <ComboBoxItem x:Uid="SettingsPage_ThemeSystemItem" Tag="Default" />
                        <ComboBoxItem x:Uid="SettingsPage_ThemeLightItem" Tag="Light" />
                        <ComboBoxItem x:Uid="SettingsPage_ThemeDarkItem" Tag="Dark" />
                    </ComboBox>
                </Grid>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>
